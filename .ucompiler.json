{
  "defaultRule": "compile-src",
  "rules": [{
    "name": "compile-src",
    "plugins": ["babel"],
    "babel": {
      "presets": ["aria"]
    },
    "include": [{
      "directory": "src",
      "extensions": ["js"]
    }],
    "outputPath": "lib/{relativePath:4}"
  }]
}
