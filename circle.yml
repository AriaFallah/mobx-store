test:
  pre:
    - npm run build
  override:
    - npm test
    - npm run lint
  post:
    - './node_modules/.bin/nyc report --reporter=text-lcov | ./node_modules/.bin/coveralls'

machine:
  node:
    version: 5.2.0
